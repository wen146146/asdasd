<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhxy.eshop.mapper.BlogMapper">
	<select id="findAll" resultType="com.xhxy.eshop.entity.Blog">
		select id, title, pic, content, createtime from blog
	</select>
	<select id="findById" resultType="com.xhxy.eshop.entity.Blog">
		select id, title, pic, content, createtime from blog where id = #{blogId}
	</select>
	<select id="findLatestBlog" resultType="com.xhxy.eshop.entity.Blog">
		select id, title, pic, content, createtime from blog order by createtime desc limit #{n}
	</select>
</mapper>
